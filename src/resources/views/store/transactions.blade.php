@extends('store.layout')
@section('content')<section class="page-head"><h1>Cek Transaksi</h1><p>Lacak pesanan dari invoice atau order ID.</p><form class="lookup"><input name="invoice" value="{{ request('invoice') }}" placeholder="Masukkan nomor invoice/order"><button class="btn">Cari</button></form></section><div class="table-card"><table><tr><th>Invoice</th><th>Game</th><th>Layanan</th><th>Tanggal</th><th>Status</th></tr>@foreach($orders as $order)<tr><td><a href="{{ route('invoice.show',$order->invoice_number) }}">{{ $order->invoice_number }}</a></td><td>{{ $order->game->name }}</td><td>{{ $order->product->name }}</td><td>{{ $order->created_at }}</td><td><span class="status {{ $order->status }}">{{ $order->status }}</span></td></tr>@endforeach</table></div>@endsection
